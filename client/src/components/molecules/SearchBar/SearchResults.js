import React, { useState } from 'react';
import {
  Box
} from '@chakra-ui/react';

const SearchResults = () => {

    return (
        <Box borderWidth='1px' m={0} p={2} maxW={400}>
            Search Results
        </Box>
    );
}

export default SearchResults;